spring:
  application:
    name: demo
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: {url}
    username: {username}
    password: {password}
  sql:
    init:
      platform: mysql
  jpa:
    show-sql: true
    #hibernate:
    #  ddl-auto: create

